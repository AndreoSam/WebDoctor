import {json} from '../src/hostedOwnership.js';import {projects,durableStoreEnabled} from '../src/supabaseStore.js';export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});try{return json(res,200,{projects:durableStoreEnabled()?await projects():[],hosted:true,durable:durableStoreEnabled()})}catch(e){return json(res,500,{error:e.message})}}

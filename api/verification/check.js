import {readBody,json} from '../../src/hostedOwnership.js';
function decode(id){try{return JSON.parse(Buffer.from(String(id),'base64url').toString('utf8'))}catch{return null}}function esc(s){return s.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}
export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});try{const p=await readBody(req),x=decode(p.id);if(!x?.origin||!x?.token)return json(res,400,{error:'Verification session is invalid. Start verification again.'});let evidence=null;try{const r=await fetch(x.origin+'/.well-known/webdoctor-verification.txt',{signal:AbortSignal.timeout(8000)});if(r.ok&&(await r.text()).includes(x.token))evidence='HTML verification file'}catch{}if(!evidence)try{const r=await fetch(x.origin,{signal:AbortSignal.timeout(8000)}),h=await r.text(),head=(h.match(/<head\b[^>]*>[\s\S]*?<\/head>/i)||[])[0]||'';if(new RegExp(esc(x.token),'i').test(head))evidence='Meta tag'}catch{}if(!evidence)return json(res,200,{verified:false,error:'Verification token was not found yet.'});return json(res,200,{id:p.id,origin:x.origin,hostname:new URL(x.origin).hostname,token:x.token,verified:true,evidence,verificationMethod:evidence,methods:{htmlFile:x.origin+'/.well-known/webdoctor-verification.txt',meta:`<meta name="webdoctor-verification" content="${x.token}">`}})}catch(e){return json(res,500,{error:e.message})}}
